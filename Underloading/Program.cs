using ArrayUtils;
using System;

namespace Underloading
{
    public static class Program
    {
        public static void Main(string[] args)
        {
            int[] intArr = {1, 9, 4, 18, 7, 53};
            double[] doubleArr = {1.1, 9.9, 4.4, 18.18, 7.7, 53.53};

            // double avg = averaging.Average(intArr);
            // Console.WriteLine("The average of your int array is: " + avg);

            // int max = macks.Max(intArr);
            // Console.WriteLine("The max of your int array is: " + max);

            // int min = minnie.Min(intArr);
            // Console.WriteLine("The min of your int array is: " + min);

            // int product = productivity.Product(intArr);
            // Console.WriteLine("The product of your int array is: " + product);

            // int sum = summarizing.Sum(intArr);
            // Console.WriteLine("The sum of your int array is: " + sum);
        }
    }
}