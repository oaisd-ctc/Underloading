using StringUtils;
using ArrayUtils;

namespace Underloading {
    public static class Program {
        public static void Main(string[] args)
        {
            // Console.WriteLine(ToUpper.Yell());
            // Console.WriteLine(ToLower.Whisper());
            // Console.WriteLine(Length.HowLong());
            // Console.WriteLine(CountVowels.NumberOfVowels());
            // Console.WriteLine(CountConstants.NumberOfConstants());
            // Console.WriteLine(HasLetter.IncludesLetter());
            // Console.WriteLine(ToHTML.StringToHTML());
            // Console.WriteLine(ToFortnite.StringToWeLikeFortnite());
            // Console.WriteLine(OutputToString.ConvertToString(ToCharArray.StringToCharArray()));
            // ToFile.PasteToFile();
        }
    }
}