using System;
using StringUtils;  // The StringUtils namespace. Job: Lukas Batema
//using ArrayUtils; // The ArrayUtils namespace, based off of the template I made on StringUtils. Job: Pluto Bohl
//using MathUtils; // The MathUtils namespace, based off of the template I made on StringUtils. Job: Matty Dozeman

namespace Underloading {
    public static class Program {
        public static void Main(string[] args)
        {
            //Console.WriteLine(ToUpper.Yell());
            //Console.WriteLine(ToLower.Whisper());
            //Console.WriteLine(Length.HowLong());
            //Console.WriteLine(CountVowels.NumberOfVowels());
            //Console.WriteLine(CountConstants.NumberOfConstants());
            Console.WriteLine(HasLetter.IncludesLetter());
        }
    }
}